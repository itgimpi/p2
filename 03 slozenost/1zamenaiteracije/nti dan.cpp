#include<bits/stdc++.h>
using namespace std;

 

int main() {
    //ios_base::sync_with_stdio(false); cin.tie(0);
    int n, a, d; cin >> n;
    
    while ( cin >> a >> d ) {
        int zadnji = a + ( n - 1 ) * d;


        
        cout << zadnji << endl;

     }
    return 0; }